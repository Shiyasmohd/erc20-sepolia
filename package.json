{
  "name": "erc20-sepolia",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ erc20-sepolia",
    "create-local": "graph create --node http://localhost:8020/ erc20-sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ erc20-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 erc20-sepolia",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.70.0",
    "@graphprotocol/graph-ts": "0.35.0",
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
